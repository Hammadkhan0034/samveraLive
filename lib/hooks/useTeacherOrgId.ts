'use client';

/**
 * @deprecated Use useCurrentUserOrgId instead
 * Re-exported for backward compatibility during migration
 */
export { useCurrentUserOrgId as useTeacherOrgId } from './useCurrentUserOrgId';

